CREATE TABLE `vhc_margenutilidad` (
  `IDMARGENUTILIDAD` BINARY(16) NOT NULL,
  `IDANIO` BINARY(16) NOT NULL,
  `PERIODOANIO` TINYINT NOT NULL,
  `PERIODOMES` TINYINT NOT NULL,
  `PERIODODIA` TINYINT NOT NULL,
  `BODYMODELCPOS` VARCHAR(255) NOT NULL,
  `VERSIONARCHIVO` VARCHAR(255) DEFAULT NULL,
  `PRECIOLISTA` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOCREDITO` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOCONTADO` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOLISTAUTILIDADSINIMPUESTOMONTO` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOLISTAUTILIDADSINIMPUESTOPORC` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOCREDITOUTILIDADSINIMPUESTOMONTO` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOCREDITOUTILIDADSINIMPUESTOPORC` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOCONTADOUTILIDADSINIMPUESTOMONTO` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `PRECIOCONTADOUTILIDADSINIMPUESTOPORC` DECIMAL(65,2) NOT NULL DEFAULT 0.00,
  `FECHACALCULO` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`IDMARGENUTILIDAD`),  
  KEY `VHC_MARGENUTILIDAD_IDX_IDANIO` (`IDANIO`),
  INDEX `VHC_MARGENUTILIDAD_IDX_PERIODOS` (`IDANIO`,`PERIODOANIO`, `PERIODOMES`, `PERIODODIA`),
  CONSTRAINT `VHC_MARGENUTILIDAD_FK_IDANIO` FOREIGN KEY (`IDANIO`) REFERENCES `vhc_anio` (`IDANIO`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_uca1400_ai_ci;
